[package]
name = "steam-idle"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "steam-idle"
path = "src/main.rs"

[dependencies]
steamworks = "0.11"
win-process = { path = "../win-process" }
windows-sys = { version = "0.59", features = [
    "Win32_Foundation",
    "Win32_System_Threading",
] }
